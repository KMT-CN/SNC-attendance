NODE_ENV=production

# 后端端口配置（只监听 127.0.0.1，需要通过反向代理对外访问）
BACKEND_PORT=10234

# MongoDB 配置
MONGODB_URI=mongodb://admin:snc_attendance_2025@localhost:27017/attendance?authSource=admin

# JWT 配置（生产环境请务必修改）
JWT_SECRET=snc_attendance_jwt_secret_key_2025_change_in_production
JWT_EXPIRES_IN=7d

# CORS 配置（如果使用反向代理，建议配置具体域名）
CORS_ORIGIN=*

# 日志级别
LOG_LEVEL=info
